import { 
  startCounter, 
  pauseCounter, 
  resumeCounter, 
  restartCounter, 
  setCounterSpeed, 
  getCounterStatus,
  CounterSpeed,
  CounterState
} from './counterService';

export {
  startCounter,
  pauseCounter,
  resumeCounter,
  restartCounter,
  setCounterSpeed,
  getCounterStatus,
  CounterSpeed,
  CounterState
};
